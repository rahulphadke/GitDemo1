// Print alphabets in uppercase, 4 per line
for (let i = 0; i < 26; i += 4) {
    const letters = [];
    for (let j = i; j < Math.min(i + 4, 26); j++) {
        letters.push(String.fromCharCode(65 + j));
    }
    console.log(letters.join(' '));
}